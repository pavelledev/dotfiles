################
### MONITORS ###
################

monitor = DP-1, 1920x1080@165, 0x0, 1
monitor = DP-2, 1920x1080@165, 1920x0, 1

#############################
### ENVIRONMENT VARIABLES ###
#############################

# Nvidia stuff do not touch!
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = __GL_GSYNC_ALLOWED,1

# Keyboard
env = QT_IM_MODULE,fcitx5
env = XMODIFIERS,@im=fcitx5
env = INPUT_METHOD,fcitx5
env = SDL_IM_MODULE,fcitx5

# Electron
env = ELECTRON_OZONE_PLATFORM_HINT,auto

# Hyprland
env = GTK_THEME,Nordic
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

#################
### AUTOSTART ###
#################

exec-once = swww-daemon
exec-once = fcitx5
exec-once = dunst
exec-once = waybar

#####################
### LOOK AND FEEL ###
#####################

xwayland {
  force_zero_scaling = true
}

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2

    col.active_border = rgb(434C5E)
    col.inactive_border = rgb(2e3440)

    resize_on_border = false
    allow_tearing = false
    layout = dwindle
}

decoration {
    rounding = 15
    rounding_power = 2

    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 10
        render_power = 3
        color = rgba(1a1a1aee)
    }
}

animations {
    enabled = true

    bezier = overshot, 0.05, 0.9, 0.1, 1.05
    bezier = smoothOut, 0.5, 0, 0.99, 0.99
    bezier = smoothIn, 0.5, -0.5, 0.68, 1.5
    animation = windows, 1, 5, overshot, slide
    animation = windowsOut, 1, 3, smoothOut
    animation = windowsIn, 1, 3, smoothOut
    animation = windowsMove, 1, 4, smoothIn, slide
    animation = border, 1, 5, default
    animation = fade, 1, 5, smoothIn
    animation = fadeDim, 1, 5, smoothIn
    animation = workspaces, 1, 3, default
}

dwindle {
    pseudotile = true
    preserve_split = true
}

misc {
    disable_hyprland_logo = true
    font_family = Cantarell
}

#############
### INPUT ###
#############

input {
    kb_layout = us
    follow_mouse = 1
    sensitivity = 0
}

###################
### KEYBINDINGS ###
###################
$mainMod = SUPER

# Start a terminal
bind = $mainMod, RETURN, exec, kitty

# Kill focused window
bind = $mainMod SHIFT, Q, killactive

# Start app launcher
bind = $mainMod, D, exec, rofi -show drun

# Toggle floating
bind = $mainMod SHIFT, SPACE, togglefloating

# Toggle fullscreen
bind = $mainMod SHIFT, F, fullscreen

# Reload Waybar
bind = $mainMod SHIFT, P, exec, pkill waybar && hyprctl dispatch exec waybar && notify-send "Arch Linux" "Successfully reloaded waybar"

# Screenshot
bind = , PRINT, exec, grim - | wl-copy && notify-send "Arch Linux" "Screenshot taken"
bind = ALT, PRINT, exec, grim -o "$(hyprctl activeworkspace -j | jq -r .monitor)" - | wl-copy && notify-send "Arch Linux" "Monitor screenshot taken"
bind = $mainMod SHIFT, S, exec, grim -g "$(slurp)" - | wl-copy && notify-send "Arch Linux" "Regional screenshot taken" 

# Toggle layout
bind = $mainMod, P, pseudo
bind = $mainMod, J, togglesplit

# Move focus
bind = $mainMod, LEFT, movefocus, l
bind = $mainMod, RIGHT, movefocus, r
bind = $mainMod, UP, movefocus, u
bind = $mainMod, DOWN, movefocus, d

# Move focus window
bind = $mainMod SHIFT, LEFT, movewindow, l
bind = $mainMod SHIFT, RIGHT, movewindow, r
bind = $mainMod SHIFT, UP, movewindow, u
bind = $mainMod SHIFT, DOWN, movewindow, d

# Switch workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# Move active workspace to next monitor
bind = $mainMod ALT, LEFT, movecurrentworkspacetomonitor, l
bind = $mainMod ALT, RIGHT, movecurrentworkspacetomonitor, r

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod SHIFT, mouse:272, movewindow
bindm = $mainMod SHIFT, mouse:273, resizewindow

##############################
### WINDOWS AND WORKSPACES ###
##############################

# No animation on layer
layerrule = no_anim true, match:namespace grim
layerrule = no_anim true, match:namespace slurp
layerrule = no_anim true, match:namespace selection

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppress_event maximize, match:class .*

# Fix some dragging issues with XWayland
windowrule = no_focus true, match:class ^$, match:title ^$, match:xwayland true, match:float true, match:fullscreen false, match:pin false
